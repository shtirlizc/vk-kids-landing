---

---

<div class="container"><slot /></div>

<style>
  .container {
    width: 100%;
    max-width: var(--container);
    margin-inline: auto;
    padding: 0 var(--space24);

    /* desktop */
    @media (width >= 960px) {
      padding: 0 var(--space40);
    }
  }
</style>
