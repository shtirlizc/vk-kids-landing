---

---

<div class="container"><slot /></div>

<style>
  .container {
    width: 100%;
    max-width: var(--container);
    margin-inline: auto;
    padding: var(--space24);

    /* desktop */
    @media (width >= 960px) {
      padding: var(--space40);
    }
  }
</style>
