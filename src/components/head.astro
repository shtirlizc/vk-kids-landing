---
import "../styles/_main.css";

import { Font } from "astro:assets";

const faviconPath = `${import.meta.env.BASE_URL.replace(/\/?$/, "/")}favicon/`;
---

<head>
  <meta charset="utf-8" />
  <Font cssVariable="--font-vk-display" />
  <Font cssVariable="--font-vk-text" />
  <Font cssVariable="--font-vk-condensed" />

  <link
    rel="icon"
    type="image/png"
    sizes="32x32"
    href={`${faviconPath}favicon-32x32.png`}
  />
  <link
    rel="icon"
    type="image/png"
    sizes="16x16"
    href={`${faviconPath}favicon-16x16.png`}
  />
  <link rel="icon" href={`${faviconPath}favicon.ico`} sizes="any" />
  <link
    rel="apple-touch-icon"
    sizes="180x180"
    href={`${faviconPath}apple-touch-icon.png`}
  />
  <link rel="manifest" href={`${faviconPath}site.webmanifest`} />
  <meta name="viewport" content="width=device-width" />
  <meta name="theme-color" content="#dec7ff" />
  <meta name="generator" content={Astro.generator} />
  <meta property="og:image" content="https://shtirlizc.github.io/vk-kids-landing/OpenGraph.jpg" />
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:image" content="https://shtirlizc.github.io/vk-kids-landing/OpenGraph.jpg" />
  <title>Семья и дети | Детский конкурс</title>

  <!-- Top.Mail.Ru counter -->
  <script is:inline>
    var _tmr = window._tmr || (window._tmr = []);
    _tmr.push({ id: "2183087", type: "pageView", start: new Date().getTime() });
    (function (d, w, id) {
      if (d.getElementById(id)) return;
      var ts = d.createElement("script");
      ts.type = "text/javascript";
      ts.async = true;
      ts.id = id;
      ts.src = "https://top-fwz1.mail.ru/js/code.js";
      var f = function () {
        var s = d.getElementsByTagName("script")[0];
        s.parentNode.insertBefore(ts, s);
      };
      if (w.opera == "[object Opera]") {
        d.addEventListener("DOMContentLoaded", f, false);
      } else {
        f();
      }
    })(document, window, "tmr-code");
  </script>
  <!-- /Top.Mail.Ru counter -->
</head>
